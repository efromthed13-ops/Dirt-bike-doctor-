import React from 'react';

export default function SearchBox() {
  return {
    placeholder: 'Describe your bike problem',
    examples: [
      'won\'t start',
      'has spark but no run',
      'overheating'
    ]
  };
}
